# Checkov Exceptions

**Contents:** [Blind spots](#blind-spots)

Checkov scans the Terraform on every PR, and a failure blocks the merge. Every
`#checkov:skip` in the code has a line here. Longer reasons: `cmdb.yml` → `checkov`.

**Status (2026-09-24):** bootstrap 244 passed, 0 failed · databricks 15 passed, 0 failed.

| Check | Where | Why we accept it |
|---|---|---|
| CKV_AWS_145, 35, 158, 26 | Buckets, trail, log group, topic | Customer-managed KMS keys cost monthly. AWS-managed encryption + TLS enforced. Revisit with classified data |
| CKV_AWS_18 | Buckets | Access logging deferred; CloudTrail data events cover state |
| CKV2_AWS_62 | Buckets | No consumer for event notifications |
| CKV_AWS_144 | Buckets | Cross-region replication breaks the single-region decision and doubles cost |
| CKV_AWS_338 | Log group | 90 days in CloudWatch is the alarm copy; S3 keeps 365 days |
| CKV_AWS_252 | Trail | Delivery notifications aren't detection; metric filters are |
| CKV_AWS_356, 111 | Deploy role | `"*"` only where AWS has no resource type |
| CKV2_AWS_5 | Workspace SG | Databricks attaches it at runtime |
| CKV2_AWS_5, 11, 12 | Endpoint SG, VPC | False positive: Checkov can't follow `[0]` indexes. Verified in AWS |
| CKV_AWS_24, 25, 260 | Intra-cluster rules | False positive: source is the SG itself, not the internet |

## Blind spots

> Detail: [`../../cmdb.yml`](../../cmdb.yml) → `checkov.blind_spots`

- Resources behind `count = 0` are invisible. A clean scan of switched-off code
  proves nothing.
- IAM policies generated by the Databricks provider aren't scanned.
